export const dynamic = 'force-dynamic';
import { Shell, PageTitle } from '@/components/Shell';import { findPhase } from '@/lib/data';
export default async function PhasePage({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const p=findPhase(slug)||findPhase('product-positioning')!;return <Shell><PageTitle title={`Phase ${p.number}: ${p.title}`} subtitle="Implemented in local-safe form and checked by audit scripts."/><div className="box"><h2>Scope</h2><p>This phase is represented in docs, fixtures, routes, or local modules. Production wallet, fiat, and mainnet features remain disabled until their gates are satisfied.</p></div><div className="box"><h2>Audit gate</h2><p><code>npm run audit:phase</code> verifies all 30 phases exist. Additional audits check ledger balancing, safety guards, required routes, banned artifacts, and final matrix.</p></div></Shell>}