import { redirect } from 'next/navigation';import { requireAdmin } from '@/lib/auth/session';import { prisma } from '@/lib/db';import { writeAudit } from '@/lib/repositories/audit';import { queueNotification } from '@/lib/repositories/notifications';import { verifyCsrfFromForm } from '@/lib/api/csrf';
export async function POST(req:Request,context:{params:Promise<{id:string}>}){const admin:any=await requireAdmin();const {id}=await context.params;const f=await req.formData();await verifyCsrfFromForm(f);const app=await prisma.sellerApplication.update({where:{id},data:{status:'rejected',reviewedAt:new Date(),reviewedBy:admin.id}});await writeAudit(admin.username,'seller.application.reject',id,`${app.username} rejected.`);await queueNotification('email','seller.rejected',app.username,'Seller application rejected.');redirect('/admin/users')}
