export const dynamic = 'force-dynamic';
import { redirect } from 'next/navigation';import { appendAudit, appendLedgerGroup, appendMessage, appendNotification, readState, writeState } from '@/lib/local-store';
export async function POST(_:Request,context:{params:Promise<{id:string}>}){const {id}=await context.params;const state=readState();const trade=state.trades.find(t=>t.id===id);if(trade){trade.status='RESOLVED_BUYER';trade.deadline='resolved';appendLedgerGroup(state,[{account:`escrow:${id}`,direction:'debit',sats:trade.btcAmountSats,reason:'admin resolved buyer',tradeId:id},{account:`buyer:${trade.buyer}:available`,direction:'credit',sats:trade.buyerReceivesSats,reason:'buyer receives admin resolution',tradeId:id},{account:'platform:fee_revenue',direction:'credit',sats:trade.feeSats,reason:'platform fee admin resolution',tradeId:id}]);appendMessage(state,id,'system','Ranch Office resolved the dispute in favor of the buyer.',true);appendAudit(state,'ranch_office','dispute.resolve_buyer',id,'Released local fake escrow to buyer.');appendNotification(state,'email','dispute.resolved_buyer',trade.buyer,`${id} resolved in your favor.`);}writeState(state);redirect('/admin/disputes')}
