export const dynamic = 'force-dynamic';
import { redirect } from 'next/navigation';import { appendAudit, appendLedgerGroup, appendMessage, appendNotification, readState, writeState } from '@/lib/local-store';
export async function POST(_:Request,context:{params:Promise<{id:string}>}){const {id}=await context.params;const state=readState();const trade=state.trades.find(t=>t.id===id);if(trade){trade.status='RESOLVED_SELLER';trade.deadline='resolved';appendLedgerGroup(state,[{account:`escrow:${id}`,direction:'debit',sats:trade.btcAmountSats,reason:'admin resolved seller',tradeId:id},{account:`seller:${trade.seller}:available`,direction:'credit',sats:trade.btcAmountSats,reason:'seller refunded from fake escrow',tradeId:id}]);appendMessage(state,id,'system','Ranch Office resolved the dispute in favor of the seller.',true);appendAudit(state,'ranch_office','dispute.resolve_seller',id,'Returned local fake escrow to seller.');appendNotification(state,'email','dispute.resolved_seller',trade.seller,`${id} resolved in your favor.`);}writeState(state);redirect('/admin/disputes')}
