export const dynamic = 'force-dynamic';
import { Shell, PageTitle } from '@/components/Shell';import { findUser } from '@/lib/data';
export default async function Trader({params}:{params:Promise<{username:string}>}){const {username}=await params;const u=findUser(username)||findUser('btc_rancher')!;return <Shell><PageTitle title={u.username} subtitle={`${u.role} profile and reputation`}/><div className="grid"><div className="box"><h2>Public stats</h2><dl><dt>Trades</dt><dd>{u.trades}</dd><dt>Positive</dt><dd>{u.positive}</dd><dt>Negative</dt><dd>{u.negative}</dd><dt>Dispute rate</dt><dd>{(u.disputeRate*100).toFixed(2)}%</dd><dt>Avg release</dt><dd>{u.avgReleaseMinutes || '-'} min</dd><dt>Last seen</dt><dd>{u.lastSeen}</dd></dl></div><div className="box"><h2>Badges</h2><ul>{u.badges.map(b=><li key={b}>{b}</li>)}</ul></div><div className="box"><h2>Ranch warnings</h2><p>No public restrictions on this local fixture profile.</p></div></div></Shell>}