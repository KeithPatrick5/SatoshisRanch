export const dynamic = 'force-dynamic';
import { Shell, PageTitle } from '@/components/Shell';
import { MarketFilters, OfferTable } from '@/components/OfferTable';
import { PhaseList } from '@/components/PhaseList';
export default async function Home({searchParams}:{searchParams:Promise<{[key:string]: string | string[] | undefined}>}){const sp=await searchParams;return <Shell><PageTitle title="Buy Bitcoin peer-to-peer" subtitle="Old-school offer board, seller-funded escrow model, reputation first, local fake build."/><div className="notice"><b>Local safety mode:</b> this build uses fake balances, local JSON state, and disabled wallet broadcasting. It is for local testing and GitHub only.</div><MarketFilters searchParams={sp}/><OfferTable searchParams={sp}/><section className="grid"><div className="box"><h2>Escrow flow</h2><ol><li>Seller BTC is locked in local fake escrow.</li><li>Buyer pays seller directly outside the app.</li><li>Seller confirms payment.</li><li>Ranch releases fake BTC ledger credit to buyer.</li></ol></div><div className="box"><h2>Launch rule</h2><p>Invite-only sellers first. Tiny buyer limits. No open high-limit marketplace.</p></div><div className="box"><h2>Design rule</h2><p>Dense tables, small fonts, utility-first. No bubbly AI SaaS design.</p></div></section><PageTitle title="30 phase bible" subtitle="Every phase is represented in routes, docs, fixtures, local APIs, and audits."/><PhaseList/></Shell>}
