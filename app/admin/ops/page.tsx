import { requireAdmin } from '@/lib/auth/session';
export const dynamic = 'force-dynamic';
import { Shell, PageTitle } from '@/components/Shell';import { AuditLogTable, NotificationsTable, WorkerRunsTable } from '@/components/AdminTables';export default async function Ops(){await requireAdmin();return <Shell><PageTitle title="Ops dashboard" subtitle="Workers, queues, stuck timers, wallet mismatch, and cron health."/><div className="box"><h2>Run local workers</h2><form method="post" action="/api/admin/workers/run"><button>Run worker scan</button></form></div><div className="grid"><div className="box"><h2>Trade expiry worker</h2><p>Checks timers and records that no auto-release is allowed.</p></div><div className="box"><h2>Ledger reconciliation</h2><p>Runs balanced ledger verification.</p></div><div className="box"><h2>Wallet watcher</h2><p>Disabled until testnet phase.</p></div></div><div className="box"><h2>Worker runs</h2><WorkerRunsTable/></div><div className="box"><h2>Mock notifications</h2><NotificationsTable/></div><div className="box"><h2>Audit log</h2><AuditLogTable/></div></Shell>}
