# Security Notes

This repo is GitHub-safe because secrets, wallet material, env values, uploads, node modules, and generated builds are gitignored. Production auth, real DB, real uploads, and real BTC require explicit env setup and later review.
