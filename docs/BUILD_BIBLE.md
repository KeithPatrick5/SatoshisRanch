# Satoshi's Ranch 30 Phase Build Bible

This repo follows the locked Satoshi's Ranch bible. Do not stray unless there is an infrastructure, design, engineering, safety, or practical issue.

## Phase 1: Product definition, public positioning, and local-only constraints

Slug: `product-positioning`

Local implementation: route/doc/module/fixture/audit gate included. Real BTC custody is disabled unless a later audited wallet service is added.

## Phase 2: Old-school P2P marketplace layout and offer-table behavior

Slug: `localbitcoins-patterns`

Local implementation: route/doc/module/fixture/audit gate included. Real BTC custody is disabled unless a later audited wallet service is added.

## Phase 3: Offer browsing, filters, regions, currencies, and payment methods

Slug: `marketplace-foundation`

Local implementation: route/doc/module/fixture/audit gate included. Real BTC custody is disabled unless a later audited wallet service is added.

## Phase 4: Local auth model, profiles, 2FA placeholders, account status

Slug: `user-accounts`

Local implementation: route/doc/module/fixture/audit gate included. Real BTC custody is disabled unless a later audited wallet service is added.

## Phase 5: Invite-only seller applications and admin approval workflow

Slug: `seller-applications`

Local implementation: route/doc/module/fixture/audit gate included. Real BTC custody is disabled unless a later audited wallet service is added.

## Phase 6: Seller offer creation, pricing margin, limits, terms, pause/resume

Slug: `offer-management`

Local implementation: route/doc/module/fixture/audit gate included. Real BTC custody is disabled unless a later audited wallet service is added.

## Phase 7: Strict trade states and transition guardrails

Slug: `trade-state-machine`

Local implementation: route/doc/module/fixture/audit gate included. Real BTC custody is disabled unless a later audited wallet service is added.

## Phase 8: Trade room with timers, status panels, chat, timeline, and actions

Slug: `trade-room`

Local implementation: route/doc/module/fixture/audit gate included. Real BTC custody is disabled unless a later audited wallet service is added.

## Phase 9: Fake BTC balance locking and release/refund simulation

Slug: `fake-escrow`

Local implementation: route/doc/module/fixture/audit gate included. Real BTC custody is disabled unless a later audited wallet service is added.

## Phase 10: Double-entry ledger model and balanced fixture transactions

Slug: `double-entry-ledger`

Local implementation: route/doc/module/fixture/audit gate included. Real BTC custody is disabled unless a later audited wallet service is added.

## Phase 11: Local wallet shell with deposits, withdrawals, locked balances, no mainnet

Slug: `wallet-shell`

Local implementation: route/doc/module/fixture/audit gate included. Real BTC custody is disabled unless a later audited wallet service is added.

## Phase 12: Dispute opening, proof uploads, evidence timeline, and admin resolution

Slug: `dispute-center`

Local implementation: route/doc/module/fixture/audit gate included. Real BTC custody is disabled unless a later audited wallet service is added.

## Phase 13: Ranch Office admin dashboard for trades, users, wallet, and disputes

Slug: `admin-command-center`

Local implementation: route/doc/module/fixture/audit gate included. Real BTC custody is disabled unless a later audited wallet service is added.

## Phase 14: Reputation, release speed, disputes, feedback, badges, seller stats

Slug: `reputation-layer`

Local implementation: route/doc/module/fixture/audit gate included. Real BTC custody is disabled unless a later audited wallet service is added.

## Phase 15: Risk flags, clustering placeholders, velocity rules, fake receipt warnings

Slug: `risk-engine`

Local implementation: route/doc/module/fixture/audit gate included. Real BTC custody is disabled unless a later audited wallet service is added.

## Phase 16: Seller-paid trade fee calculation and visible buyer receive amount

Slug: `fee-engine`

Local implementation: route/doc/module/fixture/audit gate included. Real BTC custody is disabled unless a later audited wallet service is added.

## Phase 17: Email and Telegram notification event registry and mock outbox

Slug: `notifications`

Local implementation: route/doc/module/fixture/audit gate included. Real BTC custody is disabled unless a later audited wallet service is added.

## Phase 18: Allowed payment method policy and high-risk method restrictions

Slug: `payment-method-policy`

Local implementation: route/doc/module/fixture/audit gate included. Real BTC custody is disabled unless a later audited wallet service is added.

## Phase 19: 2FA requirements, rate limits, admin controls, sessions, audit logs

Slug: `security-controls`

Local implementation: route/doc/module/fixture/audit gate included. Real BTC custody is disabled unless a later audited wallet service is added.

## Phase 20: Typed local API routes for offers, trades, wallet, admin, and phases

Slug: `api-surface`

Local implementation: route/doc/module/fixture/audit gate included. Real BTC custody is disabled unless a later audited wallet service is added.

## Phase 21: Prisma schema for users, offers, trades, wallet, ledger, disputes

Slug: `database-schema`

Local implementation: route/doc/module/fixture/audit gate included. Real BTC custody is disabled unless a later audited wallet service is added.

## Phase 22: Local cron/worker design for timers, trade expiry, ledger reconciliation

Slug: `worker-design`

Local implementation: route/doc/module/fixture/audit gate included. Real BTC custody is disabled unless a later audited wallet service is added.

## Phase 23: Bitcoin infra abstraction, testnet-only interfaces, mainnet disabled

Slug: `bitcoin-infra-plan`

Local implementation: route/doc/module/fixture/audit gate included. Real BTC custody is disabled unless a later audited wallet service is added.

## Phase 24: Tiny limits, trusted tiers, withdrawal holds, seller exposure caps

Slug: `limits-engine`

Local implementation: route/doc/module/fixture/audit gate included. Real BTC custody is disabled unless a later audited wallet service is added.

## Phase 25: Seller dashboard, exposure, low-balance warnings, bulk offer management

Slug: `seller-tools`

Local implementation: route/doc/module/fixture/audit gate included. Real BTC custody is disabled unless a later audited wallet service is added.

## Phase 26: Immutable trade/admin/wallet/security audit timeline

Slug: `audit-logging`

Local implementation: route/doc/module/fixture/audit gate included. Real BTC custody is disabled unless a later audited wallet service is added.

## Phase 27: Operational health pages: jobs, stuck trades, wallet mismatch, queues

Slug: `ops-dashboard`

Local implementation: route/doc/module/fixture/audit gate included. Real BTC custody is disabled unless a later audited wallet service is added.

## Phase 28: Testnet readiness checklist and disabled fake testnet watcher shell

Slug: `testnet-readiness`

Local implementation: route/doc/module/fixture/audit gate included. Real BTC custody is disabled unless a later audited wallet service is added.

## Phase 29: Mainnet gates, emergency freeze, hot-wallet cap policy, manual approval

Slug: `mainnet-gates`

Local implementation: route/doc/module/fixture/audit gate included. Real BTC custody is disabled unless a later audited wallet service is added.

## Phase 30: Final self-audit matrix across all phases and safety constraints

Slug: `final-self-audit`

Local implementation: route/doc/module/fixture/audit gate included. Real BTC custody is disabled unless a later audited wallet service is added.
