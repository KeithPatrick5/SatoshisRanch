# Wallet Safety

The default adapter is `disabledWallet`. Mainnet broadcast is not available unless multiple production-only env gates are intentionally configured. Regtest and testnet adapter shells are present for later phases.
